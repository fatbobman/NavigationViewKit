    import XCTest
    @testable import NavigationViewKit

    final class NavigationViewKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(NavigationViewKit().text, "Hello, World!")
        }
    }
